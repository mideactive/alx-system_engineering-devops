# Configure web-01 so that its firewall redirects port 8080/TCP to port 80/TCP.

# set one install iptables-persistent
sudo apt-get install iptables-persistent

# Next, add a rule to redirect traffic from port 8080 to port 80 using iptables
# This rule tells the server to redirect incoming traffic to port 8080 to port 80.

sudo iptables -t nat -A PREROUTING -p tcp --dport 8080 -j REDIRECT --to-port 80


# Save the iptables rules so that they persist across reboots

sudo iptables-save > /etc/iptables/rules.v4

# Finally, start and enable iptables-persistent service

sudo systemctl start netfilter-persistent
sudo systemctl enable netfilter-persistent

